---
layout: default
---
{% include header.html %}
<main id="site-main" class="home">
    <dl>
        <dt>Posts: </dt>
            {% for post in site.posts %}<dd><a href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a><span class="muted">{{ post.date | date: "%b %-d, %Y" }}</span></dd>{% endfor %}
        <dt>Project: </dt>
            <dd><a href="/project/friday">some landing page</a></dd>
        </dl> 
</main>
{% include footer.html %}
